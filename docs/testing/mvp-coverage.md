# 단계 0–5 MVP 검증 범위

검증일: 2026-09-23  
대상 버전: `0.1.0`  
범위: `requirements/requirements.yaml`에서 단계 0–5와 `all`로 지정된 요구사항

이 문서는 자동 검사, 검증 Sol의 기능 브라우저 확인, 메인 Astra의 최종 시각 판정, 도구 제약, 실제 사용자 미검증을 구분한다.

## 최종 자동 게이트

| 검사 | 결과 |
|---|---|
| `npm test -- --run` | 7개 파일, 228개 테스트 통과 |
| `npm run typecheck` | 통과 |
| `npm run check:boundaries` | 24개 소스 파일 통과 |
| `python tools/validate_specs.py` | 통과 |
| `npm run build` | 1,882개 모듈 빌드 통과 |

빌드는 Three.js 지연 청크가 502.30 kB로 Vite의 500 kB 안내 기준을 2.30 kB 초과한다는 비차단 경고를 남긴다. 기능·타입·명세 실패는 없다.

## 요구사항별 근거

### 단계 0–1: 문서 계약과 직류 계산

| 요구사항 | 상태 | 검증 근거 |
|---|---|---|
| DAT-003 | 자동 검증 | v1 검증·복제 마이그레이션, 원본 불변, 잘못된 입력의 구조화 진단, JSON 왕복을 `specifications.test.ts`에서 확인했다. |
| SIM-001 | 자동 검증 | 명시적 단자·도선·분기점 net, 화면 좌표 교차 비연결, 입력 순서 독립성, 특수 ID를 `simulation.test.ts`에서 확인했다. |
| SIM-002 | 자동 검증 | FIX-01~10 상태와 수치, KCL·전력 보존, 기준점 이동, 극단 저항, 0 Ω·닫힌 스위치·전류계를 확인했다. |
| SIM-003 | 자동 검증 | 부품 전압·전류·전력, 전원과 수동 부호 규약, 음전압 전원, 비유한 결과 거부를 확인했다. |
| QLT-003 | 자동 검증 | 반복 입력의 바이트 동등 결과, 배열 재배열 독립성, 외부 시간·네트워크 없이 동작하는 핵심과 모듈 경계를 확인했다. |

단계 1 기능 브라우저 확인에서는 FIX-01 9 Ω의 1 A가 18 Ω 편집 후 0.5 A로 바뀌고, FIX-06/07/08이 수치를 숨긴 채 진단하며, FIX-10의 기준점만 이동하는 것을 확인했다.

### 단계 2: 편집과 로컬 저장

| 요구사항 | 상태 | 검증 근거 |
|---|---|---|
| EDT-001 | 자동+기능 브라우저 검증 | 불변 명령, 100회 이상 실행 취소·다시 실행, 추가·이동·회전·삭제의 편집 흐름을 확인했다. |
| EDT-002 | 자동+기능 브라우저 검증 | endpoint ID 보존, 이동·회전 시 경로 재계산, 직각 배선과 취소를 확인했다. |
| EDT-003 | 자동 검증 | 좌표상 교차만으로 net을 합치지 않으며 명시적 분기점만 연결함을 확인했다. |
| EDT-004 | 자동+기능 브라우저 검증 | `1k`와 `1000 Ω` 동등 파싱, 단위 검사, 잘못된 입력 시 문서 불변을 확인했다. |
| EDT-005 | 자동+기능 브라우저 검증 | 복수 선택 복사의 새 ID, 포함된 도선·분기점·주석만 복사, 삭제 시 참조 정리를 확인했다. |
| EDT-006 | 자동+기능 브라우저 검증 | 이동·회전 뒤 연결 endpoint는 유지하고 기존 waypoint를 폐기해 다시 배선하는 동작을 확인했다. |
| DAT-001 | 자동+기능 브라우저 검증 | 자동·수동 저장 키 분리, 이전 자동 저장 백업 복구, quota 실패 시 기존 저장본 보존, 새로고침 복구를 확인했다. |
| DAT-002 | 자동 검증 | 유효 v1 직렬화·파싱, 잘못된 JSON·버전·의미 참조 거부, 실패 시 저장소 불변을 확인했다. |

### 단계 3: 전위 시각화

| 요구사항 | 상태 | 검증 근거 |
|---|---|---|
| VIS-001 | 자동+기능 브라우저 검증 | 같은 net의 endpoint가 같은 `PotentialValue`를 공유하고 net당 한 숫자 callout을 표시함을 확인했다. |
| VIS-002 | 자동+기능 브라우저 검증 | 동일 전위의 동일 색, 범위 고정 시 회로 간 색 일치, 미정 전위의 회색 표현을 확인했다. |
| VIS-003 | 기능 브라우저 검증 | 단자·도선 선택 시 같은 net의 도선·단자·분기점을 함께 강조함을 확인했다. |
| VIS-004 | 자동+기능 브라우저 검증 | 2D x-y 보존, `z=scale×(V-reference)`, 열린 스위치 간격, 미정 전위 높이 제외, 3D 재맞춤을 확인했다. |
| VIS-005 | 자동+기능 브라우저 검증 | 제안 경로, 연속 사용자 경로, 비연속 경로 거부, 부품별 전위 변화와 양방향 hover를 확인했다. |
| VIS-006 | 기능 브라우저 검증 | 전류 화살표 방향·크기 분리와 표시 토글을 확인했다. |
| VIS-007 | 자동+기능 브라우저 검증 | 자동 범위와 고정 min/max, 기준점 이동에 따른 높이 이동과 전압차 불변을 확인했다. |

### 단계 4: 측정과 진단

| 요구사항 | 상태 | 검증 근거 |
|---|---|---|
| SIM-004 | 자동+기능 브라우저 검증 | SOURCE_SHORT, FLOATING_SUBCIRCUIT, CONFLICTING_SOURCES 등 안정 코드, 관련 ID, 수치 미제공과 한국어 수정 안내를 확인했다. |
| SIM-005 | 자동+기능 브라우저 검증 | 1 V 시험 전원, 포트 전원 분리, 나머지 독립 전원 0 V 치환, 유한·개방·단락 상태와 FIX 부하값을 확인했다. |
| SIM-006 | 자동+기능 브라우저 검증 | KCL/KVL 항·부호·합·허용오차, 열린 경로와 비연속 경로 거부를 확인했다. |
| MEA-001 | 자동+기능 브라우저 검증 | 빨강−검정 부호, 탐침 반전, 같은 net의 0 V, 기준점 불변, 오류 회로의 수치 숨김을 확인했다. |
| MEA-002 | 자동+기능 브라우저 검증 | 임시 전류계 직렬 삽입, 방향, 원본 문서 불변, 직렬·병렬 전류를 확인했다. |
| MEA-003 | 자동+기능 브라우저 검증 | 문서 snapshot·조건·예측·시간·단위를 고정한 기록, RFC CSV quoting과 수식 주입 방어를 확인했다. |
| MEA-004 | 자동+기능 브라우저 검증 | 최대 100개 표본 제한, 원시 x/y·단위, 실패 표본 null, UI 21개 표본 그래프와 수치 범위를 확인했다. |

### 단계 5: 문제지와 출력

| 요구사항 | 상태 | 검증 근거 |
|---|---|---|
| TCH-001 | 자동+기능 브라우저 검증 | 같은 `CircuitDocument`에서 문제/정답을 전환하고 값·이름·전압·전류의 실제 값, 숨김, `?`, 빈칸, 문자를 적용했다. 표시 metadata 전후 compiled circuit와 계산 결과가 동등함을 확인했다. |
| TCH-002 | 자동+기능 브라우저 검증 | 점 이름·오른쪽 화살표·미지수·설명, always/problem/answer/hidden 가시성, anchor 중첩 배치, 추가·수정·삭제·undo를 확인했다. 주석 전후 연결망과 계산 결과가 동등하다. |
| TCH-003 | 자동+기능 브라우저 검증 | 결정론적 독립 SVG, XML escape·제어문자 대체, 흑백·투명·여백·숫자 형식, 빈 회로·회전·긴 텍스트 bounds, PNG 배율과 클립보드 성공/거부 대체를 확인했다. |

단계 5 기능 브라우저 확인에서 흑백·투명·여백 64·4× 옵션으로 실제 PNG를 생성했다. 정답지는 2,876×1,812 px, 문제지는 주석을 포함해 2,876×1,908 px로 디코딩되었다. 투명 checkerboard, 흑백 기호, 문제용 `R_A`·`?`·전압 빈칸·화살표와 정답용 실제 값·설명을 확인했다. SVG·PNG 저장 완료 안내와 PNG 클립보드 성공 안내도 확인했다. 390×844에서 작업 모드, 실제 PNG 모달, 수업 화면 패널 숨김, 가로 넘침 없음도 확인했다. 이 기능 브라우저 근거는 검증 Sol이 수행했다.

메인 Astra는 최종 시각 판정에서 데스크톱의 회로 만들기, 2D·3D 전위, 6 V 탐침 측정, 문제지 PNG를 확인했다. 390×844 모바일에서는 2D·3D와 PNG 전체 폭을 확인했고, 페이지 폭은 `374/374`로 가로 넘침이 없었다. 최종 기본 출력 PNG는 2,061×1,335 px로 디코딩되었으며 오른쪽 잘림 없이 표시되었다.

Chrome 자동화 확장의 비동기 message channel 종료 오류가 1건 기록되었으나 앱 stack이 없고 사용자 흐름 실패도 없었다. 앱 자체 오류로 분류하지 않았다.

## 도구 제약으로 남긴 항목

| 항목 | 현재 근거 | 남은 확인 |
|---|---|---|
| 브라우저 JSON 파일 열기 | 파서·스키마·저장 불변성은 자동 테스트 통과 | 파일 선택 broker가 없어 실제 file chooser 왕복은 확인하지 못했다. |
| 저장한 파일 다시 읽기 | SVG 문자열·PNG Blob·CSV 내용은 자동 테스트, PNG는 앱 미리보기에서 실제 디코딩 | 브라우저가 내려받은 JSON/SVG/PNG/CSV 파일을 자동화 도구로 다시 열어 byte 단위 비교하지 않았다. |
| 클립보드 거부 시 UI 다운로드 | `copyPng`의 API 부재와 권한 거부가 같은 PNG Blob을 반환함을 자동 테스트 | 사용한 Chrome에서는 클립보드 쓰기가 성공했으므로 실제 브라우저 거부 안내와 다운로드 파일은 재현하지 않았다. |

도구 제약 항목은 관련 순수 모듈의 실패 경로를 자동 테스트했지만 실제 운영체제 파일 선택기·다운로드함·클립보드 거부 UI를 대체한다고 주장하지 않는다.

## 사람 대상 미수행 항목

- 실제 교사가 회로 문제지 그림을 2분 안에 만드는지 관찰하지 않았다.
- 실제 학생이 전위·전위차·전류 방향·3D 높이의 의미를 설명할 수 있는지 연구하지 않았다.
- 터치 기기, 실제 프린터, 다른 워드프로세서에 PNG 붙여넣기를 시험하지 않았다.
- 교실 환경의 수업 진행, 장시간 사용, 접근성 보조기술 사용자 검증은 단계 6–7 후속 범위다.

따라서 이 문서는 단계 0–5 기능 계약과 자동·기능 브라우저 게이트 통과를 기록하며, 교사·학생 사용성이나 교육 효과를 입증하지 않는다.


## 후속 VIS-004: 3D 기준면과 전압 안내

`tests/potential-3d.test.ts`에 음수/미소 전압 눈금, 0 V 기준면을 포함하는 범위, net별 전위 표지와 미정 전위 제외, 선택 단자의 부호 있는 전압 차이/높이 일치 4개 검증을 추가했다. 기존 물리 불변식 테스트와 함께 232개가 통과했다. 메인 직접 브라우저 검증 범위와 한계는 `mvp-tracker.md` 후속 항목에 기록했다.
