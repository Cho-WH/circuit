# MVP 작업 추적

목표는 단계 0~5의 1차 MVP다. 각 단계의 종료 조건을 검증한 뒤 다음 단계로 이동한다.

## 역할과 검증 절차

- 메인 Astra: 스켈레톤, 공개 계약, 물리 해석 핵심, 통합 및 완료 판정.
- 구현 GPT-5.6 Sol: 지정된 파일과 모듈 구현. 별도 테스트·빌드·검증을 실행하지 않는다.
- 검증 GPT-5.6 Sol: 메인의 지시에 따라 테스트 작성·실행, 타입·빌드 등 코드 검증만 수행한다.
- 사용자의 최신 지시에 따라 최종 브라우저 시각 검증은 메인 Astra가 직접 수행한다. 이전 단계의 Sol 브라우저 결과는 당시 수행자로 기록한다.
- 공통 타입·의존성 변경은 메인이 조정하고 같은 파일의 동시 수정을 피한다.
- 체크는 산출물 존재와 검증 결과를 구분하며, 실패·미검증 항목은 완료로 표시하지 않는다.

## 진행

| 단계 | 상태 | 완료 근거 |
|---|---|---|
| 0 기반 설계 | 완료 | 78 테스트, 타입·경계·빌드·명세 검사 통과 |
| 1 물리 시제품 | 완료 | 123 테스트 및 기준 회로·값 변경 브라우저 확인 |
| 2 편집기 핵심 | 완료 | 160 테스트 및 데스크톱·모바일 편집 흐름 |
| 3 전위 시각화 | 기능 완료 | 170 테스트 및 2D·3D·경로·모바일 브라우저 확인 |
| 4 측정과 진단 | 완료 | 211 테스트 및 측정·임시 계기·기록·실험 브라우저 확인 |
| 5 교사 출력 | 기능 완료 | 228 테스트, 실제 PNG·복사 및 메인 최종 시각 검증 |

## 첫 세로 조각: FIX-01

- [x] 9 V 전원과 9 Ω 저항 문서의 검증·왕복 보존
- [x] 명시적 연결망에서 1 A 및 9 W 계산
- [x] 회로 선택과 저항값 입력이 계산 결과에 즉시 반영
- [x] SVG와 절점 전위 표시
- [x] 자동 저장과 다시 열기
- [x] 자동 테스트와 브라우저 흐름 검증

관련 요구사항: SIM-001~003, EDT-004, VIS-001, DAT-001~002. 관련 규약: PV-01~02, FIX-01, ADR-003~004.

### 단계 1 실험 작업

- 0 Ω: 직렬 닫힌 스위치와 전류계는 0 V 제약으로 전류를 구한다. 병렬 중복 경로에서는 유일하지 않은 전류를 꾸며내지 않고 진단한다. 전원과 0 Ω 경로 충돌을 구분한다. 관련 ADR-003.
- 행렬 풀이: 작은 조밀 MNA 행렬에 피벗 선택을 적용한다. 특이·불일치·나쁜 조건에서 결과를 거부하고 유효 해의 잔차를 검사한다. 기준점 변경 불변식, 저항 크기 차이, FIX-01~10으로 비교한다. 관련 ADR-004.
- 계산 엔진 계약을 구현한 뒤, 회로 선택·값 변경 데모로 데이터→연결망→계산→표시 흐름을 검증한다.

## 검증 기록

검증 담당의 실제 실행 결과를 단계 완료 시 추가한다.

- 단계 0: 검증 Sol이 Vitest 2개 파일 78개 테스트, TypeScript, 모듈 경계(7개 소스), Vite 빌드, Python 명세 검증을 실행해 모두 통과했다. 금지 import·순환 의존·내부 import에 대한 실패 테스트도 포함했다.
- 단계 1: 3개 파일 123 테스트, 타입·경계·명세·빌드 검사 통과. 브라우저에서 FIX-01의 1 A→저항 18 Ω 변경 후 0.5 A, FIX-06/07/08의 진단과 수치 숨김, FIX-10의 기준점 변경을 확인했다. 브라우저 경고·오류 0, npm audit 0건.
- 단계 2: 4개 파일 160 테스트, 타입·경계(16 소스)·빌드·명세 통과. 데스크톱에서 새 회로→배선→값 수정→복사·정렬·회전·이동·실행 취소·기준점·분기점·새로고침 복구를 확인했다. 390×844 모바일에서 겹침·가로 넘침 없이 이름과 값이 읽혔다. 파일 선택기의 자동화 broker 문제로 실제 브라우저 업로드 검사는 수행하지 못했으며, 유효·무효 파일 파싱과 저장 보존은 모듈 테스트로 검증했다. Blob 다운로드 이벤트 수집도 도구에서 확인되지 않았으므로 다운로드 파일 자체의 브라우저 검증은 후속 확인 대상으로 남긴다.

- 단계 3: 5개 파일 170 테스트, 타입·경계(19 소스)·명세·빌드(1877 모듈) 통과. 2D 숫자·색·전류, 고정 범위, 기준점 변경, 등전위, 제안/직접 경로와 양방향 강조를 확인했다. 3D 카메라·축척·예제 변경 시 재맞춤, FIX-05 열린 스위치 단절, FIX-07 전위 미정과 높이 제외를 확인했다. 모바일 390×844 페이지 가로 넘침 없이 읽힘. Three 지연 청크 501.87 kB(압축 126.72 kB)의 비차단 크기 경고가 있다. 앱 기원 콘솔 오류는 없었고 확장 프로그램 메시지 채널 오류 2건은 앱과 구분했다. 실제 학생·교사의 의미 이해와 시간 목표는 미검증이다.

- 단계 4 중간: 측정 모듈 집중 검사 41개 통과. 브라우저에서 FIX-02 ±6 V, R2 임시 전류계 1 A, 부하 9 Ω/전체 회로 0 Ω, 기록·CSV 트리거, KCL·KVL, 21개 조건 그래프를 확인했다. 모바일 기록 버튼의 흰 바탕/흰 글자 문제를 수정했으며 최종 검증 중이다.

- 단계 4 최종: 6개 파일 211 테스트, 타입·경계(22 소스)·명세·빌드(1880 모듈) 통과. FIX-06/07/08에서 수치 미정과 한국어 진단, 모바일 기록 버튼 대비 수정, 390×844 가로 넘침 없음과 콘솔 오류 0을 확인했다. CSV 다운로드 동작은 트리거까지 확인했으며 실제 파일 내용은 모듈 테스트로 검증했다.

- 단계 5 중간: 표기·주석·출력 집중 검사 14개와 타입 검사 통과. 브라우저에서 문제/정답 표시, 주석 편집·실행 취소·다시 실행, 흑백·투명·여백 64·4배 PNG를 확인했다. 실제 PNG는 정답 2876×1812, 문제 2876×1908로 디코딩되었고 투명 배경과 문제 표기가 확인됐다. 클립보드 쓰기는 성공했으며 외부 문서 붙여넣기는 미검증이다.

- 최종 시각 검증(메인 Astra): 데스크톱의 낮은 창에서 전위 화면이 작아지고 상태줄이 밀리는 점을 수정했다. 초기 회로 맞춤, 좁은 세로 공간의 그래프 높이·축 글자, 주석 충돌 회피를 보정했다. 3D의 최소 높이와 카메라 투영 경계 맞춤을 수정해 전위 지도 상단 잘림을 해결했다. 측정 탐침 +/−와 6 V 표시, 문제지의 물음표·빈칸·주석 및 실제 PNG를 직접 확인했다. 모바일 390×844의 2D/3D, 네 모드 탭과 PNG를 확인하고 미리보기 오른쪽 잘림을 고쳤다. 실제 PNG 2061×1335 디코딩 및 페이지 client/scroll width 374/374를 확인했다.

- 최종 코드 게이트(검증 Sol): 7개 파일 228 테스트, 타입 검사, 모듈 경계 24 소스, 명세 검증, 1882 모듈 빌드 통과. Three 지연 청크 502.30 kB(압축 126.87 kB)의 비차단 크기 경고가 있다. `git diff --check`에서 공백 오류는 없었다. 마지막 시각 수정 후 전체 코드 게이트를 다시 통과했다.

## 완료와 후속 작업

단계 0~5 기능 MVP를 완료했다. 최종 시각 검증은 사용자의 변경 지시에 따라 메인 Astra가 직접 수행했고, 이후 검증 Sol은 코드 검사만 수행했다. Git 원격은 `https://github.com/Cho-WH/circuit.git`, 브랜치는 `codex/mvp`이며 구현은 작업 트리에 보존했다. 배포·push·PR은 하지 않았다.

[요구사항별 검증 범위](../testing/mvp-coverage.md)에 실제 사용자 연구와 도구 제한을 정리했다. [빠른 시작](../ux/quick-start.md)에서 실행·사용·저장·출력을 안내한다. 공유 및 학생 활동 배포는 단계 6, 실제 교실 사용성 검증은 단계 7의 후속 작업이다.


## MVP 후속 — 3D 전위 연속성 개선

- [x] 원본 SVG 기호·배선·라벨의 투명 0 V 바닥 회로도
- [x] V 눈금·수직 안내선·net 표지·부품 선택과 부호 있는 ΔV
- [x] 음의 전위·미정 전위·열린 스위치 표시 규약 유지
- [x] 유한한 펼침/카메라 전환 및 움직임 감소 지원
- [x] 3D 작업 공간 확대·경로 그래프 펼치기·표지 충돌 처리
- [x] 서브 에이전트 없이 메인 직접 구현
- [x] 최종 타입·물리 테스트·경계·빌드·브라우저 검증

검증 결과: Vitest 8개 파일 232개 테스트 통과. TypeScript·모듈 경계(25개 소스)·명세 검증·프로덕션 빌드 통과. 메인이 Chrome에서 기본 직렬 회로, 저항 선택의 9 V → 6 V / ΔV 3 V, 사선·정면·위에서 보기, 기준점 이동의 음의 전위, 열린 스위치 단절 및 양단 9 V, 경로 그래프 펼침/접힘, 390×844 모바일 너비의 전체 회로 표시를 확인했다. 검증용 예제 교체는 실행 취소하여 원래 회로·주석을 복원했다.

빌드는 Three.js 지연 로드 청크 약 510 kB에 대한 기존 500 kB 크기 경고가 남는다. 초기 HMR 중 옛 props 계약 오류는 전체 재진입/새로고침 뒤 재현되지 않았다. 최종 새로고침 후 앱 예외는 관찰되지 않았고 브라우저 메시지 채널 경고만 관찰했다. 움직임 감소 분기는 구현했으며 실제 OS 설정 전환 검증은 수행하지 않았다.


## 후속 — 측정 작업 공간 분리

- [x] 편집 도구 대신 측정 도구·탐침·결과를 상단에 배치
- [x] 회로 클릭과 가지 선택 동기화, 측정 시 구조·값 편집 차단
- [x] 기록·법칙 확인·실험을 보조 패널로 이동, 모드 전환 시 기록 유지
- [x] 긴 안내 없이 탐침 선택/연결 상태 표현
- [x] 데스크톱·모바일 시각 확인: 메인 직접 수행
- [x] 전체 236개 테스트, 측정 UI 접근성·미연결 상태·부호 회귀 포함
- [x] 타입·모듈 경계·명세 검사 및 Pages 빌드
- 배포는 이 변경의 GitHub Actions 실행과 공개 사이트에서 최종 확인한다.

직렬 회로 R1 양단 3 V, 기록 추가, 전류 측정 1 A, 회로에서 R2 선택과 가지 목록 동기화, Delete 키의 문서 비변경, 편집 복귀 후 실행 취소 비활성(원본 유지), 다시 측정 진입 시 기록 유지, 390×844에서 도구·탐침·측정값·회로가 함께 보이는 배치를 확인했다.
